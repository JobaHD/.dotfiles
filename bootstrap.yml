- name: Bootstrap development environment
  hosts: localhost

  tasks:
  - name: Install packages with dnf
    become: yes
    ansible.builtin.dnf:
      name:
        - git
        - stow
        - neovim
        - tmux
        - fzf
        - ripgrep
        - fd-find
        - zsh
      state: present
